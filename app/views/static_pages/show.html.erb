<div class="custom">
  <h1>
    <%= @hello %>
  </h1>
  <img src='<%= asset_path("sample-image.gif") %>'>
  <% @users.each do |user| %>
    <h3><%= "#{user.name} : #{user.birthday}" %></h3>
  <% end %>
</div>